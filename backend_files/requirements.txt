# ===========================================================================
# FireOps SIH2026 — Core runtime
# ===========================================================================
# Everything needed to run the API and all six pipeline stages. The heavy
# deep-learning stack is optional and lives in requirements-v2.txt: without it
# Stages 3, 4 and 5 fall back to their NumPy / analytic implementations and
# the system stays fully functional, reporting itself as degraded.

# --- API layer -------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
websockets>=12.0

# --- Data and numerics -----------------------------------------------------
# The Planck inversion in Stage 2 is pure NumPy and is the only hard
# numerical dependency of the physics gate.
numpy>=1.26
pandas>=2.2
requests>=2.31

# --- Legacy classifiers (retained as the Stage 6 fallback path) ------------
scikit-learn>=1.4
lightgbm>=4.3
joblib>=1.4
