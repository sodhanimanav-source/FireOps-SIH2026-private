__pycache__/
*.pyc
.env
venv/
.venv/
.vscode/

# --- 6-stage pipeline runtime artefacts ------------------------------------
# The intelligence store is regenerated by the agent on every run and is
# machine-local state, not source.
data/intelligence.sqlite3
data/intelligence.sqlite3-shm
data/intelligence.sqlite3-wal

# Stage caches (INSAT frames, OSM subgraphs, vision tiles). All are
# reconstructible from their upstream sources.
data/cache/insat/
data/cache/osm_graph/
data/cache/vision/

# Hugging Face weight cache for Prithvi-EO-2.0 (~1.2 GB)
.cache/
hf_cache/
