# ===========================================================================
# FireOps SIH2026 — Deep-learning and geospatial stack (optional)
# ===========================================================================
# Install on top of requirements.txt to enable the trained-model paths:
#
#   Stage 3  torch_geometric SAGEConv      (else: NumPy GraphSAGE)
#   Stage 4  Prithvi-EO-2.0 via transformers (else: NDVI/NBR/NDBI analysis)
#   Stage 5  torchdiffeq Neural ODE        (else: explicit RK4 in NumPy)
#
# Every stage detects at import time whether its dependency is present and
# degrades cleanly if not — the pipeline never fails because a wheel is
# missing, it only reports lower confidence.
#
#   pip install -r requirements.txt -r requirements-v2.txt
#
# CPU-only install (much smaller, sufficient for inference):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Deep learning ---------------------------------------------------------
torch>=2.2
torch_geometric>=2.5          # Stage 3: GraphSAGE message passing
torchdiffeq>=0.2.3            # Stage 5: Neural ODE integration
transformers>=4.40            # Stage 4: Prithvi-EO-2.0 loader
huggingface_hub>=0.23         # Stage 4: foundation model weights

# --- Geospatial ------------------------------------------------------------
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
rasterio>=1.3                 # HLS / Sentinel-2 tile reading for Stage 4
pyarrow>=15.0

# --- Utilities -------------------------------------------------------------
tqdm>=4.66
pytest>=8.0
